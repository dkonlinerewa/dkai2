<?php
if (!defined('CONFIG_LOADED')) {
    die('Direct access not allowed');
}

if (!defined('DB_HOST')) { define('DB_HOST', 'localhost'); }
if (!defined('DB_NAME')) { define('DB_NAME', 'u182854778_target2030'); }
if (!defined('DB_USER')) { define('DB_USER', 'u182854778_djyoti2030'); }
if (!defined('DB_PASS')) { define('DB_PASS', 'DkRewa@#1995'); }
if (!defined('DB_CHARSET')) { define('DB_CHARSET', 'utf8mb4'); }
if (!defined('DB_PORT')) { define('DB_PORT', 3306); }

if (!defined('ENVIRONMENT')) { define('ENVIRONMENT', 'development'); }

if (!defined('APP_SECRET_KEY')) { define('APP_SECRET_KEY', 'generate_your_own_random_64_character_secret_key_here_for_production'); }
if (!defined('CSRF_TOKEN_SECRET')) { define('CSRF_TOKEN_SECRET', 'generate_your_own_random_64_character_csrf_secret_key_for_security'); }

if (!defined('SESSION_LIFETIME')) { define('SESSION_LIFETIME', 24 * 60 * 60); }
if (!defined('MAX_LOGIN_ATTEMPTS')) { define('MAX_LOGIN_ATTEMPTS', 5); }
if (!defined('LOCKOUT_TIME')) { define('LOCKOUT_TIME', 15 * 60); }
if (!defined('API_RATE_LIMIT')) { define('API_RATE_LIMIT', 100); }
if (!defined('PASSWORD_MIN_LENGTH')) { define('PASSWORD_MIN_LENGTH', 8); }
if (!defined('TOKEN_EXPIRY')) { define('TOKEN_EXPIRY', 3600); }

if (!defined('MAX_FILE_SIZE')) { define('MAX_FILE_SIZE', 10 * 1024 * 1024); }

if (!defined('SMTP_HOST')) { define('SMTP_HOST', 'smtp.hostinger.com'); }
if (!defined('SMTP_PORT')) { define('SMTP_PORT', 587); }
if (!defined('SMTP_USER')) { define('SMTP_USER', 'noreply@hidk.in'); }
if (!defined('SMTP_PASS')) { define('SMTP_PASS', 'DkRewa@#1995'); }
if (!defined('SMTP_FROM')) { define('SMTP_FROM', 'noreply@hidk.in'); }
if (!defined('SMTP_FROM_NAME')) { define('SMTP_FROM_NAME', 'D K Careers'); }

if (!defined('ADMIN_EMAIL')) { define('ADMIN_EMAIL', 'care@hidk.in'); }
if (!defined('SUPPORT_EMAIL')) { define('SUPPORT_EMAIL', 'support@hidk.in'); }
